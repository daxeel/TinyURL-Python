#!/usr/bin/env python

from distutils.core import setup

setup(name='tinyurl',
      version='1.0',
      description='Python module to create short links from tinyurl.com',
      author='Daxeel Soni',
      author_email='sayhi@daxeelsoni.in',
      url='http://www.daxeelsoni.in',
      keywords = ['url', 'shortner', 'tinyurl'],
      py_modules=['tinyurl'],
     )